---
title: "算法竞赛基础·C++常用操作"
author: MayL
date: 2026-02-10
categories: ["算法与数据结构", "算法"]
tags: ["算法", "学习笔记"]
render_with_liquid: false
description: "本文整理“算法竞赛基础·C++常用操作”的核心思路、典型问题与实现要点，便于刷题复习和后续查阅。"
---

@[toc]
# IO操作
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/02020cd743284e558782b4493c752e65.png)

## 无数据规模IO：`while (cin >> x)`/`while(scanf("%d",&x))`



```cpp
int x;
void solve() {
	int sum = 0;
	while (cin >> x) {
		sum += x;
	}
	cout << sum;
}
```
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/f75215778f2e40709f962405096612a2.png)
+ `scanf("%d",&x)`默认返回**读取输入的个数**。
```cpp
void solve() {
	int sum = 0;
	while (scanf_s("%d",&x)) {
		sum += x;
	}
	cout << sum;
}
```

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/8064ad4927f24244b870c7f98fe943d0.png)
### 不同操作系统终止输入快捷键：ctrl + z/d
+ **使用`\n + ctrl+z \n`**：在windows下终止输入(手动输入)
+ **使用`ctrl+d`**：在linux下终止输入。


## 输入和输出文件
[参考](https://www.bilibili.com/video/BV1XV411W7uG/?spm_id_from=333.1007.top_right_bar_window_history.content.click&vd_source=e9a92f1200efca1791b7c6c0dd5e0c2c)
### VS 2022读取文件配置
+ **首先`data.in`必须与`*.vcxproj`同一目录之下**
+ **必须添加该文件到项目中去**，否则无法可视化修改文件或者可能无法读取文件(即使路径正确).
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/324e17b69ebf422a96b587a31a28ff1f.png)
### DevC++读取文件配置
+ 基本思路一致：**同一路径之下，确保处于同一项目之下**。

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/302b43010f7c42edb104328abd2995c2.png)

### DevC++编译器选项：支持C++11语法(很重要!)
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/037429a13aa6401ba632fb9d53dcc070.png)


---
### 重定向输入
+ **将键盘输入定向为stdin，将标准输出定向为文件**。
+ 所有`scanf`,`printf`,`cin`,`cout`都会被重定向处理。

```cpp
	freopen("data.in", "r", stdin);
	freopen("data.out", "w", stdout);
```

### 手动控制文件写入和写出



#  `lower_bound`：二分查找
+ **查找原则是`a[mid] > = target`的左边界**，也就第一个大于等于target的数，返回是对应的内存地址
+ 注意：需要减去起始地址得到下标`idx`
+ ### 离散化：获得新下标用法：``idx = lower_bound(a.begin()+1,a.begin()+len+1,a[i])-a``
教程：[C++ lower_bound()函数用法详解](https://c.biancheng.net/view/7521.html)

# `unique`：去重函数
+ 对**已经排序**的数组a进行元素去重处理，**左闭右开**，返回的是去重后的数组**最后一个元素的下一个元素**的迭代器或者内存地址。
+ ### 前置条件：数组必须有序。
+ ### 获得去重后的数组长度用法：`newlen = unique(a+1,a+alen+1)-a-1`。
教程：[unique函数用法讲解](https://www.cnblogs.com/wangkundentisy/p/9033782.html)


# `priority_queue<type,vector<type>,cmp>`：优先级队列
+ 第一个是类型，第二是实现容器，一般使用vector
+ 第三个是函数，**不可以是lambda表达式和普通的函数**，需要定义一个类，**重载函数调用**`( )`实现以上功能。
```cpp
class cmp{
public:
	bool operator()(type a, type b){
		return a>b; 
	}
};
```
+ **对于一般的数据类型，可以使用`greater<type>`和`less<type>`函数来实现比较**。


# `stoi(string s)`，`atoi(const char *)`和`to_string(int)`字符数组/字符串-整型
## string转int
+  `stoi(string s)`：只需要接受一个`string`格式的**字符串**
## char*转int
+ `atoi`只能接受一个`const char * `类型的**C语言字符数组**，**需要首先使用`stirng.c_str()`转换为C语言字符串进行处理。**

## char*混合转int
+ 不能直接使用atoi
```cpp
if (isdigit(s[i])) {
	int num = 0;
	while (i<s.size()&&isdigit(s[i])) {
		num *= 10;
		num += s[i] - '0';
		i++;
	}
	i--;
	nums.push(num);
}
```


# `memset(void*,val,sizeof void*)`：填充
## 填充为极大值：`0x3f`
+ **按照字节**填充数组：例如val=0x3F=0011，对于int类型数组来说，**所有值都是4个字节**，那么所有值都会被填充为`0x3f3f3f3f`。
+ **大于255的填充值会被截断，变成无效值**。

## 填充为0：`0x00`
## 填充为-1：`0xff`

# 字符串的操作
## 剪切为子串：`s.substr(pos,len)`
+ `src.substr(size_type _Off = 0,size_type _Count = npos)`：子串
+ 适用于string，从位置pos开始，长度为n的子串。
+ `src.substr()`：**不要直接使用substr**。

# 链表：`list<type>`
## 操作基础：`list<type>::iterator`
## 删除某个位置的节点：`list.erase(iterator)`，只能接受`迭代器`
## 增删节点：`push_back/front`和`pop_front/back`，接受的对象为`对应类型`

## 迭代器
### 特殊迭代器`list.begin()`和`list.end()`，前者可以与int加法，后者不能

### ⭐获得某一迭代器的前驱：`prev(list<type>::iterator)`
+ ❌获得最后一个元素：`list.end()-1`
+ ✔获得最后一个元素：`prev(list.end())`
# 哈希表`map/unordered_map`
## 哈希表删除元素：`map.earse(key)`
## 哈希表默认值：`int:0`，`string:""`

## ⭐哈希表查找函数：`iterator it = mp.find()`
### 返回值：`pair<type_a,type_b>::it`返回指向两个元素的迭代器
### 通过返回值获得元素：`auto it=mp.find()`和`it->first/it->second`
## 哈希表键值查询：`type_b = mp[type_a]`
### 返回值：返回值类型的元素
